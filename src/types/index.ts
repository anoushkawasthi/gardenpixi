/** Runtime domain types — expand to mirror TRD §15 and Zustand slices. */

export type CameraTransitionLevel = 1 | 2 | 3;

export type SectionId = "landing" | "hub" | "cottage" | "projects" | "terminal" | "contact";

export type ModalId = "none" | "recruiter" | "contact" | "resume" | "projectDetail";
