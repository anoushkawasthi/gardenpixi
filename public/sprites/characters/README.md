# Characters (Phase 13)

Export **`hub.json`** + PNG here (Aseprite JSON hash). Frame names should include **`char`**, **`avatar`**, **`npc`**, **`gate`**, **`prop`**, **`component`**, **`fence`**, **`path`**, etc. — see [.docs/CURATED_PIXEL_ASSETS.md](../../.docs/CURATED_PIXEL_ASSETS.md).

If this file is missing, the loader tries `public/sprites/components/hub.json` next.
