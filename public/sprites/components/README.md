# Components (Phase 13)

Optional second atlas: **`hub.json`** + PNG for fence segments, path tiles, props. Used only if `public/sprites/characters/hub.json` is not found. See [.docs/CURATED_PIXEL_ASSETS.md](../../.docs/CURATED_PIXEL_ASSETS.md).

You can instead ship **everything** from one atlas under `characters/` and leave this folder empty.
