/** PRD §8 Level 1 — world navigation (camera pan) duration window (ms). */
export const CAMERA_LEVEL1_MS_MIN = 600;
export const CAMERA_LEVEL1_MS_MAX = 800;
/** TRD §4.3 default — midpoint of PRD range. */
export const CAMERA_LEVEL1_MS_DEFAULT = 720;

/** Level-2 focus (project plant / cottage door) — slightly snappier than L1. */
export const CAMERA_LEVEL2_MS_MIN = 500;
export const CAMERA_LEVEL2_MS_MAX = 700;
export const CAMERA_LEVEL2_MS_DEFAULT = 600;
